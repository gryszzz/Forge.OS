{
  "name": "forge-os-ui",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc -b --pretty false",
    "build": "npm run typecheck && vite build",
    "build:offline": "npm run build",
    "verify:domain": "node scripts/verify-domain-config.mjs",
    "smoke": "node scripts/smoke.mjs",
    "domain:check": "node scripts/domain-health.mjs",
    "domain:watch": "node scripts/domain-watch.mjs",
    "preview": "vite preview",
    "ci": "npm run verify:domain && npm run typecheck && npm run build && npm run smoke"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "recharts": "^2.12.7"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.3",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
